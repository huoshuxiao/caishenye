spring:
  application:
    name: octopus
#  data:
#    mongodb:
#      host: localhost
#      database: kitty

server:
  port: 19000
  servlet:
    context-path: /${spring.application.name}

logging:
  file:
    name: log/${spring.application.name}.log
    max-history: 2
    max-size: 100MB
  level:
    #    org:
    #      apache:
    #        http: debug
    com:
      sun:
        caishenye: debug

################### 项目属性 ###############################
job:
  cron:
    fund: 0 0 0/1 * * ?