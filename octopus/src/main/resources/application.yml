spring:
  application:
    name: octopus
#  data:
#    mongodb:
#      host: localhost
#      database: kitty

server:
  port: 19000
  servlet:
    context-path: /${spring.application.name}

#logging:
##  file:
##    name: log/${spring.application.name}.log
##    max-history: 2
##    max-size: 1000MB
#  level:
##    org:
##      apache:
##        http: debug
#    com:
#      sun:
#        caishenye: debug

################### 项目属性 ###############################
job:
  cron:
    # 每小时一次
#    fund: 0 30 0/1 * * ?
#    stock: 0 15 0/1 * * ?
    # 2020-03-27 02:01:00 每天2点
    fund: 0 45 0 * * ?
    stock: 0 45 1 * * ?
mm:
  # true: 股息二次计算(股息累加)
  cal2: true